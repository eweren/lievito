<script lang="ts">
  // Landing page – temporäres Skelett, ersetzt durch echten Hero + CTA
  // sobald der Direktteig-Rechner steht.
</script>

<main class="container">
  <section class="hero">
    <p class="kicker">Antipasto</p>
    <h1>Lievito</h1>
    <p class="lead">Die Pizzateig-Werkstatt. Lokal, offlinefähig, ohne Account.</p>
  </section>
</main>

<style>
  .hero {
    padding-block: var(--space-12);
    text-align: center;
  }
  .kicker {
    color: var(--color-accent);
    font-family: var(--font-mono);
    font-size: var(--text-sm);
    text-transform: uppercase;
    letter-spacing: 0.1em;
    margin-bottom: var(--space-2);
  }
  .lead {
    color: var(--color-text-muted);
    margin-top: var(--space-4);
    font-size: var(--text-lg);
  }
</style>
